name: Update seqr Version
on:
  push:
    branches:
      - main
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: rmeneely/update-yaml@v1
        with:
          infile: charts/seqr/Chart.yaml
          varlist: appVersion=v1.0-toBeUpdated  # todo: get seqr version

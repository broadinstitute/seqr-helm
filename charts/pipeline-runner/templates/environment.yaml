apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}
  labels:
    {{- include "pipeline-runner.labels" . | nindent 4 }}
data:
  {{- with .Values.environment }}
    {{- range $key, $value := . }}
      {{ $key }}: {{ tpl $value $ | quote }}
    {{- end }}
  {{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipeline-runner.fullname" . }}
  labels:
    {{- include "pipeline-runner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipeline-runner.selectorLabels" . | nindent 4 }}
